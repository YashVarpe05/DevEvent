---
phase: 4
plan: 5
wave: 3
---

# Plan 4.5: External Artifacts & Automated Verification

## Objective
Generate secure ticket signatures, incorporate email confirmation templates (Resend), and perform comprehensive integration testing across the Registration engine.

## Context
- `lib/email.ts`
- `__tests__/registration.integration.test.ts`

## Tasks

<task type="auto">
  <name>Ticket Generation & Email Hooks</name>
  <files>
    - lib/utils/ticket.ts
    - lib/email.ts
  </files>
  <action>
    - Create `ticket.ts` export functions `generateTicketCode()` (e.g., DEV-AB12CD34 format) and `generateQrPayload()` (signed server token).
    - Update the `/api/events/[id]/register` route to append these values.
    - Stub out a transactional email call using Resend inside the register route (for Registration confirmed / canceled). Add minimalist HTML string templates in `lib/email.ts` for these two triggers.
  </action>
  <verify>QR payload generator executes cleanly without exposing raw database IDs.</verify>
  <done>Tickets are cryptographically signed and emails are triggered correctly post-registration.</done>
</task>

<task type="auto">
  <name>Integration Testing</name>
  <files>
    - __tests__/registration.integration.test.ts
  </files>
  <action>
    - Write supertest/vitest assertions for:
      - Register success for eligible free event.
      - Duplicate registration blocked.
      - Sold out event blocks new registration.
      - Cancelled/unpublished event blocks registration.
      - Attendee access control for viewing details.
      - Organizer access control for attendee list.
      - Check-in API updates status timestamp correctly.
  </action>
  <verify>Run the integration suite via `vitest`.</verify>
  <done>All edge cases for the Registration Engine pass successfully.</done>
</task>

## Success Criteria
- [ ] Ticket payloads are secure.
- [ ] API invokes the transactional email layer on success.
- [ ] Test coverage hits the target goals outlined in the prompt.
