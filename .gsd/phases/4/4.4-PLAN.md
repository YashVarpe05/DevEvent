---
phase: 4
plan: 4
wave: 2
---

# Plan 4.4: Organizer UI - Attendee Management

## Objective
Build the organizer dashboard screens for viewing attendee lists, exporting to CSV, and processing check-ins.

## Context
- `app/organizer/events/[id]/attendees/page.tsx`
- `app/organizer/events/[id]/check-in/page.tsx`

## Tasks

<task type="auto">
  <name>Implement Attendee Management Dashboard</name>
  <files>
    - app/organizer/events/[id]/attendees/page.tsx
    - components/organizer/AttendeeTable.tsx
  </files>
  <action>
    - Create a data table connecting to `GET /api/organizer/events/[id]/attendees`.
    - Display columns: Name, Email, Status Badge, Booking Date, Ticket Code, Check-in Status.
    - Add a search/filter bar.
    - Add an "Export CSV" button connecting to the export API route.
    - Provide a quick "Check In" action button within the table rows for valid tickets.
  </action>
  <verify>Page renders successfully on standard browser dimensions and handles data dynamically.</verify>
  <done>Organizers can visually track and export attendee data.</done>
</task>

<task type="auto">
  <name>Implement Dedicated Check-in View</name>
  <files>
    - app/organizer/events/[id]/check-in/page.tsx
  </files>
  <action>
    - Create an optimized, mobile-friendly check-in view.
    - Include a large input field for "Fast Ticket Code Input".
    - Upon entering a code, fetch/verify the ticket via API and mark as checked-in automatically if valid.
    - Display large, clear Success/Error states ("Already checked in", "Invalid Ticket").
  </action>
  <verify>Fast input form successfully processes a check-in claim.</verify>
  <done>Dedicated page is ready for event day scanning.</done>
</task>

## Success Criteria
- [ ] Attendee table operates smoothly with check-in badges.
- [ ] Export CSV initiates a download.
- [ ] Mobile-friendly check-in view handles quick code entry.
