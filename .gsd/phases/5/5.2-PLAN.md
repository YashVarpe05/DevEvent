---
phase: 5
plan: 2
wave: 2
---

# Plan 5.2: Ticket Types & Pricing Engine

## Objective
Implement tiered ticket management for organizers and a server-side pricing engine with platform fee logic.

## Context
- `database/ticket-type.model.ts`
- `lib/pricing.ts`
- `app/organizer/events/[id]/tickets/page.tsx`

## Tasks

<task type="auto">
  <name>Implement Ticket Type CRUD</name>
  <files>
    - app/api/organizer/events/[id]/tickets/route.ts
    - app/api/organizer/events/[id]/tickets/[ticketId]/route.ts
  </files>
  <action>
    - GET: List all ticket types for an event.
    - POST: Create a new ticket type (tier) with price, quantity, and sales window.
    - PATCH: Update existing ticket types (e.g., pause sales, update quantity).
    - DELETE: Archive/remove unused ticket tiers.
  </action>
  <verify>Routes are owner-authenticated and validate pricing input (non-negative cents).</verify>
  <done>Organizers can configure multiple paid and free ticket tiers.</done>
</task>

<task type="auto">
  <name>Develop Core Pricing Engine</name>
  <files>
    - lib/pricing.ts
    - __tests__/payments/pricing.test.ts
  </files>
  <action>
    - Implement `calculateOrderPricing({ items, organizerPlan })`.
    - Handle platform fee calculation (e.g., 5% base).
    - Snapshots each component: `Subtotal`, `PlatformFee`, `Total`.
    - Ensure integer-safe arithmetic (all calculations in cents).
  </action>
  <verify>Unit tests in `pricing.test.ts` pass for various ticket/quantity scenarios.</verify>
  <done>Reliable, audit-safe pricing logic is available globally.</done>
</task>

<task type="auto">
  <name>Build Ticket Management UI</name>
  <files>
    - app/organizer/events/[id]/tickets/page.tsx
    - components/organizer/TicketTierForm.tsx
  </files>
  <action>
    - Create a dashboard view for organizers and a form to add/edit ticket tiers.
    - Display "Sold X/Y" progress bars for each tier.
  </action>
  <verify>UI allows adding a $10.00 ticket with 100 capacity.</verify>
  <done>Organizers have a visual interface for ticket inventory.</done>
</task>

## Success Criteria
- [ ] Ticket tiers are correctly saved and retrieved from DB.
- [ ] Pricing engine accurately handles rounding and fee math.
- [ ] UI provides real-time feedback on ticket availability.
