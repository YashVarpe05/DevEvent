---
phase: 5
plan: 3
wave: 3
---

# Plan 5.3: Checkout Flow & Order Creation

## Objective
Implement a secure checkout flow for paid tickets, including order creation and Stripe Checkout redirection.

## Context
- `lib/stripe.ts`
- `app/api/checkout/create-session/route.ts`
- `components/events/TicketSelector.tsx`

## Tasks

<task type="auto">
  <name>Build Frontend Ticket Selector</name>
  <files>
    - components/events/TicketSelector.tsx
  </files>
  <action>
    - Create a multi-tier quantity selector for the event detail page.
    - Show live availability and "Sold Out" labels.
    - Validate quantity limits (min/max per order).
  </action>
  <verify>Component displays correctly on high-fidelity designs.</verify>
  <done>User can select 2 tickets for $100 total.</done>
</task>

<task type="auto">
  <name>Implement Checkout Session API</name>
  <files>
    - app/api/checkout/create-session/route.ts
  </files>
  <action>
    - Fetch event and ticket tiers.
    - Validate inventory availability using a temporary lock or `quantitySold < quantityTotal` check.
    - Create `Order` record in `pending_payment` status.
    - Initialize Stripe Checkout Session using `destination[account]` and `application_fee_amount`.
    - Return Stripe session URL.
  </action>
  <verify>POST request returns a valid Stripe checkout URL with correct pricing.</verify>
  <done>Order is created on-the-fly and user is ready to pay.</done>
</task>

<task type="auto">
  <name>Develop Success & Confirmation Hub</name>
  <files>
    - app/checkout/success/page.tsx
    - app/my/orders/[id]/page.tsx
  </files>
  <action>
    - Show order confirmation state after successful redirect.
    - Provide "View Tickets" button pointing to Order details.
  </action>
  <verify>Success page handles empty/invalid session IDs gracefully.</verify>
  <done>Post-payment user experience is seamless.</done>
</task>

## Success Criteria
- [ ] Order records are created with a `pending_payment` status.
- [ ] Checkout redirect correctly identifies the organizer's Connected Account.
- [ ] User receives a valid confirmation after payment.
