---
phase: 6
plan: 5
wave: 3
---

# Plan 6.5: Secure Cron & Background Automation

## Objective
Establish a secure endpoint for cron services (like Vercel Cron) to hit, triggering background maintenance tasks such as updating event statuses.

## Context
- .gsd/phases/6/SPEC.md

## Tasks

<task type="auto">
  <name>Cron Endpoint</name>
  <files>app/api/cron/route.ts</files>
  <action>
    - Create a GET handler at `/api/cron/route.ts`.
    - Secure it by checking the `Authorization` header against a `CRON_SECRET` environment variable (Bearer token).
    - Implement a job to transition published events to `completed` if their `endDate` is in the past.
    - Implement a job to expire/release pending orders that have been stuck in `pending_payment` for > 30 minutes, releasing their ticket inventory.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Cron endpoint successfully processes maintenance tasks securely.</done>
</task>

## Success Criteria
- [ ] Cron endpoint returns 401 without correct secret.
- [ ] Cron endpoint successfully updates stale records.
