---
phase: 6
plan: 2
wave: 1
---

# Plan 6.2: Checkout Integration for Promo Codes

## Objective
Enable attendees to apply promo codes during the checkout process and handle discounts in Stripe Checkout, including free-tier bypass for 100% off codes.

## Context
- .gsd/phases/6/SPEC.md
- app/api/checkout/create-session/route.ts
- lib/pricing.ts

## Tasks

<task type="auto">
  <name>Promo Code Application Endpoint</name>
  <files>
    app/api/checkout/validate-promo/route.ts
    lib/pricing.ts
  </files>
  <action>
    - Create a validation endpoint `/api/checkout/validate-promo` to accept `code` and `eventId`.
    - Check if code exists, is active, within dates, and `currentUses < maxUses`.
    - Return the discount details (type, value) if valid.
    - Update `lib/pricing.ts` to calculate discounts on ticket amounts correctly.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Promo code validation logic is implemented.</done>
</task>

<task type="auto">
  <name>Stripe Checkout Discount Integration</name>
  <files>
    app/api/checkout/create-session/route.ts
    app/events/[slug]/page.tsx
  </files>
  <action>
    - Update `TicketSelector` UI in `app/events/[slug]/page.tsx` to include a promo code input field.
    - Pass valid promo code to `/api/checkout/create-session`.
    - In `create-session` route, fetch the PromoCode, apply the discount to line items using Stripe Coupons/Discounts OR by adjusting unit prices. If 100% off, bypass Stripe completely and create registration directly.
    - If a promo code is used, increment its `currentUses` in the webhook or immediate path (for 100% off).
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Checkout applies promo codes correctly and bypasses payment for 100% discount.</done>
</task>

## Success Criteria
- [x] Attendees can apply promo codes in the UI.
- [x] Checkout prices reflect discounts.
- [x] 100% off codes bypass payment and successfully register attendees.
