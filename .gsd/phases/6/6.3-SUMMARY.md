# Plan 6.3 Summary: Referral Links & Tracking Schema

## Completed Tasks

### Referral Tracking Schema
- Created `database/referral.model.ts` — organizer-facing referral links with clicks/conversions tracking
- Created `database/user-referral.model.ts` — peer-to-peer user referral tracking (clicked → signed_up → converted)

### Registration Referral Attribution
- Order model (`database/order.model.ts`) includes `referralId` and `referralCode` fields
- Checkout `create-session` route accepts `ref` parameter and passes it in Stripe metadata
- Webhook fulfillment captures referral code and increments conversion counters

## Verification
- TypeScript compiles cleanly
- Both referral models exported from `database/index.ts`
